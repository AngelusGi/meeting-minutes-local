# meeting-minutes-local
Local-first meeting minutes from OBS recordings: ffmpeg + faster-whisper + Ollama, with optional Azure AI Foundry (MAI-Transcribe) transcription. Simple web UI, .md/.docx output, nothing leaves your machine by default.
